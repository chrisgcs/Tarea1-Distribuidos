version: '3'

services: 
  server:
    build: .
    volumes:
      - ./server:/usr/src/app
    ports:
      - 10000:5000
  node1:
    build: .
    volumes:
      - ./node1:/usr/src/app
    ports:
      - 5000:10000
    depends_on:
      - server